import { Theme } from "@mui/material";

export const heroStyles = (theme: Theme) => ({
    root: {
        display: 'flex',
        alignItems: 'center',
        width: '100vw',
        height: '100vh'
    },
})